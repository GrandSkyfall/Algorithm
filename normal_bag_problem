N, K = map(int,input().split())

item = []
for _ in range(N):
    W, V = map(int, input().split())
    item.append(W,V)

dp = ([0] * (K+1))

for W,V in item:
    for w in range (K,W-1,-1):
        dp[w] = max(dp[w],dp[w-W]+V)

print(dp[K])
